export enum WebRTCKey {}
export enum WebRTCEvent {
    ICE_CANDIDATE = 'ice_candidate',
    OFFER = 'offer',
    ANSWER = 'answer',
    JOIN = 'join',
    LEAVE = 'leave',
    START = 'start',
    STOP = 'stop'
}
